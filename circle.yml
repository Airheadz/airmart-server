test:
  override:
    - echo "test"
    # This allows faking out of circle

deployment:
  master:
    branch: master
    commands:
      - chmod +x scripts/deploy.sh
      - ./gradlew shadowJar
      - ./scripts/deploy.sh